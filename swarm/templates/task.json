{
  "type": "object",
  "required": ["id", "title", "description", "acceptance_criteria", "depends_on", "agent_model"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique numeric task identifier"
    },
    "title": {
      "type": "string",
      "description": "Short descriptive title for the task"
    },
    "description": {
      "type": "string",
      "description": "Detailed implementation description including file paths, function signatures, and approach"
    },
    "acceptance_criteria": {
      "type": "string",
      "description": "Bullet-pointed list of testable conditions that define 'done'"
    },
    "depends_on": {
      "type": "array",
      "items": { "type": "string" },
      "description": "IDs of tasks that must complete before this one can start"
    },
    "agent_model": {
      "type": "string",
      "enum": ["sonnet", "opus", "haiku"],
      "description": "Claude model to use for this task"
    }
  }
}
