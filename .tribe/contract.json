{
  "core_question": "Can UserType be constructed from a User model instance using UserType.from_model()?",
  "entities": [
    {
      "name": "UserType",
      "table": "N/A (GraphQL type, not DB table)",
      "created_by_task": "1",
      "key_fields": [
        "agent_tools",
        "agent_workflow_style",
        "human_agent_ratio",
        "_skills",
        "_owned_projects",
        "_tribes"
      ]
    }
  ],
  "relationships": [
    {
      "from": "UserType",
      "to": "SkillType",
      "type": "has_many",
      "via": "_skills private field resolved by skills() method",
      "created_by_task": "1"
    },
    {
      "from": "UserType",
      "to": "ProjectType",
      "type": "has_many",
      "via": "_owned_projects private field resolved by owned_projects() method",
      "created_by_task": "1"
    },
    {
      "from": "UserType",
      "to": "TribeType",
      "type": "has_many",
      "via": "_tribes private field resolved by tribes() method",
      "created_by_task": "1"
    }
  ],
  "core_queries": [
    {
      "description": "Fetch a single user by username with all relationships",
      "traversal": "UserType.from_model(user, skills=user.skills, projects=user.owned_projects, tribes=user.tribes)",
      "answers": "Can UserType be constructed from a User model instance using UserType.from_model()?"
    },
    {
      "description": "Fetch builders list with skills",
      "traversal": "UserType.from_model(user, skills=user.skills)",
      "answers": "Can UserType be constructed from a User model instance using UserType.from_model()?"
    }
  ]
}
